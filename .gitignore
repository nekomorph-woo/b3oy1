# OS
.DS_Store
**/.DS_Store

# Editor
*.swp
*.swo
*~

# Python
__pycache__/
*.pyc

# Node
node_modules/

# IDE
.idea/

# Claude Code runtime
.claude/scheduled_tasks.json
.claude/worktrees/

# git worktree 家目录（见 .claude/rules/throwaway-worktree-convention.md）
.fiber/worktrees/

# distill 临时 clone（防御性，正常在 /tmp 下）
matt-src/

# distill --check HTML 报告产物（本地查看用，每次覆盖，不入库）
# 仅忽略默认文件名；--check-out 自定义路径由用户自行管理
distill-check.html
